<!DOCTYPE html>
<html lang="en">
  {{> head}}
	<body class="g-sidenav-show bg-gray-200">
		{{#if isLogin}}
			
		{{else}}
  	{{> sidebar}}
		{{/if}}
		{{{body}}}
		{{> siteSettings}}
		{{> foot}}
	</body>
</html>